<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhao.mirrorfromgentleman">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:name=".ui.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.activity.MainActivity" />
        <activity android:name=".ui.activity.adornimage.AdornImageDetailsActivity" />
        <activity android:name=".ui.activity.buydetails.MyAllAddressActivity" />
        <activity android:name=".ui.activity.buydetails.AddAddressActivity" />
        <activity android:name=".ui.activity.buydetails.ModifiedAddressActivity" />
        <activity android:name=".ui.activity.buydetails.OrderDetailsActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <meta-data
            android:name="design_width"
            android:value="768"></meta-data>
        <meta-data
            android:name="design_height"
            android:value="1280"></meta-data>
    </application>

</manifest>